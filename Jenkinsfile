
@Library('sonar@master') _
pipeline {
    agent any
    stages { 
        stage('git checkout') {
           steps {
           checkoutSource(
           ) 
           }
        }
        
       }
    }
